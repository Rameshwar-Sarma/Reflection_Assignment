<UserControl x:Class="MVVMAssignment.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Name:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtName" Grid.Row="0" Margin="100,0,10,0" VerticalAlignment="Center"/>

        <Label Content="Age:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAge" Grid.Row="1" Margin="100,0,10,0" VerticalAlignment="Center"/>

        <Label Content="Email:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="txtEmail" Grid.Row="2" Margin="100,0,10,0" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Content="Save" Click="Save_Click" Margin="0,0,5,0"/>
            <Button Content="Cancel" Click="CancelClick"/>
        </StackPanel>


    </Grid>

</UserControl>